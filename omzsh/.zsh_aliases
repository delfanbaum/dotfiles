# tmux (because who needs the extra omzsh plugin stuff?)

alias ta='tmux attach -t'
alias ts='tmux new-session -s'
alias tl='tmux list-sessions'

# convenience aliases
alias be="bundle exec"
alias py="python"
